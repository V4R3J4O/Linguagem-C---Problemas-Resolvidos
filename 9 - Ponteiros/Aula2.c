#include <stdio.h>
#include <stdlib.h>

int main (void)
{
    int *p;
    char *p2;
    float *p3;
    printf("Tamanho: %d\n",sizeof(p));
    printf("Tamanho: %d\n",sizeof(p2));
    printf("Tamanho: %d\n",sizeof(p3));
}